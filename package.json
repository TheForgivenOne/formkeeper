{
  "name": "formkeeper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy && wrangler d1 execute formkeeper-db --file=./schema.sql --remote",
    "db:init": "wrangler d1 execute formkeeper-db --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^5.20260726.1",
    "itty-router": "^5.0.24"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
